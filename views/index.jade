#container
  #header
    h1 CommunityJS.org
  #nav
    ul
      li 
        a(href= '/') User Groups
      li
        a(href= 'http://beerjs.com/') BeerJS
      li
        a(href= '/about') About
  #main
    h2 JavaScript User Groups & Conferences
    #map_canvas
    ul
      - for (var continent in tree)
        li= continent
          ul
            - for (var country in tree[continent])
              li= country
                ul
                  - for (var state in tree[continent][country])
                    - if(state == 'no-state')
                      - for (var i = 0; i < tree[continent][country][state].length; i++)
                        li!= tree[continent][country][state][i]
                    - else 
                      li= state
                        ul
                          - for (var i = 0; i < tree[continent][country][state].length; i++)
                            li!= tree[continent][country][state][i]
    #footer 
    | Want to add/change a group?
    a(href="https://groups.google.com/group/communityjs/") Let us know!